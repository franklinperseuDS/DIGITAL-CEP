Projeto em php para pegar cep


para usar o phpunit deve navegar pelo seguinte caminho
>: vendor\bin\phpunit 

php_codesniffer usando:
>: vendor/bin/phpcs --version
phpcs -> mostra as validações
>: vendor/bin/phpcbf src/ --standard=PSR12  
o phpcbf ele deixa o arquivo no formato padrão de forma automatica